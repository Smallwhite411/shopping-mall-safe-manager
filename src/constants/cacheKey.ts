const SYSTEM_NAME = "xdragon.top"

/** 缓存数据时用到的 Key */
class CacheKey {
  static TOKEN = `${SYSTEM_NAME}-token-key`
  static SIDEBAR_STATUS = `${SYSTEM_NAME}-sidebar-status-key`
  static USERNAME = `${SYSTEM_NAME}-username-key`
}

export default CacheKey
